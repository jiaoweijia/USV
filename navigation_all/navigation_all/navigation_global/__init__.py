#import sys
#from pathlib import Path

## 将项目根目录添加到Python路径
#shared_root = Path(__file__).parent.parent  # 假设your_module.py在common目录下
#if shared_root not in sys.path:
#    sys.path.append(shared_root)